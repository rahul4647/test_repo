# ───────────────────────────────────────────────────────────────
# .env.example — Generated by ProductionAI DevOps Agent
#
# This file documents all environment variables used by the application,
# classified by storage tier. Copy to .env.local and fill in real values.
# NEVER commit .env.local or any file containing real secrets.
# ───────────────────────────────────────────────────────────────

# ─── Runtime Secrets (stored in AWS Secrets Manager — NEVER commit real values) ───
NEXT_CLERK_WEBHOOK_SECRET=your-secret-value-here
MONGODB_URL=your-secret-value-here

# ─── Next.js (Runtime Config) ───
NEXT_PUBLIC_SERVER_URL=http://localhost:3000

# ─── Clerk Auth (Runtime Secret + Build Config) ───
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_...
CLERK_SECRET_KEY=sk_test_...
NEXT_CLERK_WEBHOOK_SECRET=whsec_...

const bulkUpdateUsers = async (communityId, communityUsers) => { const bulkOps = communityUsers.map((user) => ({ updateOne: { filter: { _id: user._id }, update: { $pull: { communities: communityId } } } })); await User.bulkWrite(bulkOps); };